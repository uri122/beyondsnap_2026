// R2에 업로드하는 파일들의 캐시 정책.
// presigned URL 서명에 이 값이 포함되기 때문에, 서버(URL 생성)와 클라이언트(실제 PUT 요청)
// 양쪽에서 반드시 똑같은 값을 써야 해요. 하나만 바꾸면 서명이 안 맞아서 업로드가 실패해요.
//
// 파일명에 타임스탬프가 포함돼 있어 URL 자체가 절대 안 바뀌는(불변) 구조라서,
// 브라우저가 이후 재방문 때 다시 요청하지 않고 캐시에서 바로 씁니다.
export const UPLOAD_CACHE_CONTROL = "public, max-age=31536000, immutable";
